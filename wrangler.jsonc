{
  // Worker de assets estáticos (landing page BUMP) — sem código de Worker,
  // só serve os arquivos HTML da raiz. Nome precisa bater com o projeto na
  // Cloudflare (Workers & Pages → bump-lp).
  "name": "bump-lp",
  "compatibility_date": "2026-06-08",
  "assets": {
    "directory": "./"
  }
}
